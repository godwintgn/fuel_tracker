#!/bin/sh
# Strip Cursor co-author trailer if the agent git integration adds one.
MSG_FILE="$1"
[ -f "$MSG_FILE" ] || exit 0
grep -v '^Co-authored-by: Cursor <cursoragent@cursor.com>$' "$MSG_FILE" > "${MSG_FILE}.tmp" && mv "${MSG_FILE}.tmp" "$MSG_FILE"
